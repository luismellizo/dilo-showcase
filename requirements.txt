# Django Core
Django==5.2.8
python-dotenv==1.0.0
requests

# Django REST Framework
djangorestframework==3.14.0
djangorestframework-simplejwt
django-cors-headers

# WebSocket Support
channels==4.0.0
channels-redis==4.1.0

# AI / LLM Integration
openai==1.54.3
anthropic==0.39.0

# Async Task Queue (Celery)
celery==5.3.6
redis==5.0.3

# Image Processing (para ImageField)
# Image Processing (para ImageField)
Pillow>=10.2.0

# PDF de menú → render de páginas a imagen para el extractor de visión
pymupdf>=1.24

# WebSocket Support (Daphne)
daphne

# Database
psycopg[binary]>=3.2,<4

whitenoise[brotli]>=6.6

# Observabilidad (se activa solo si SENTRY_DSN está definido)
sentry-sdk[django]>=2.0

# Production Server (opcional)
# gunicorn==21.2.0
# uvicorn==0.24.0

# Testing
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0

# Development Tools (opcional)
# django-debug-toolbar==4.2.0
# ipython==8.18.0

# Google Sign-In (verificación de ID tokens de "Continuar con Google")
google-auth>=2.0
